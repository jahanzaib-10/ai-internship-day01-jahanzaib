# AI Internship Day 01 - Python Diagnostic Assignment

## Project Overview

This repository contains the solution and documentation for my AI Internship Day 1 diagnostic tasks. In this project, I completed coding and practice for Python basics, functions, data structures (such as dictionaries and lists), text cleaning, and error handling.

## Learning Outcomes

- Learned how to write modular functions in Python.
- Implemented error handling using `try-except`, `ValueError`, and `TypeError`.
- Gained a solid understanding of string manipulation and dictionary operations.
- Learned how to perform automated code testing using `pytest`.

## Repository Structure

- `src/`: Contains my main Python code file (`python_diagnostic.py`).
- `tests/`: Contains the pytest testing file (`test_python_diagnostic.py`).
- `docs/`: Stores my reflection document and output screenshots.
- `notebooks/`: Contains the exploratory Jupyter notebook for practice (`python_diagnostic_exploration.ipynb`).
- `requirements.txt`: Lists the required project libraries (such as pytest).

## Environment Setup

1. Open your project folder in VS Code.
2. Ensure that Python is installed on your system.
3. Open the terminal to create a virtual environment if needed.

## Installation

Run the following command in the terminal to install dependencies:

```bash
python -m pip install -r requirements.txt
Running the Project
To run the Python program, use the following command:

Bash
python src/python_diagnostic.py
To run the notebook, open notebooks/python_diagnostic_exploration.ipynb in VS Code or Jupyter Notebook.

Running Tests
To test the code, use the following command:

Bash
pytest
Python Diagnostic Summary
Task 1: Calculated student score statistics (average, highest, lowest, pass/fail counts).

Task 2: Assigned letter grades (A to F) to each score based on specific criteria.

Task 3: Converted sentences to lowercase, removed punctuation, and extracted the top 3 most frequent words.

Task 4: Cleaned student records by filtering out invalid data or none values.

Task 5: Added error handling to prevent program crashes.

Challenges Faced
Had a little difficulty getting the words to split correctly while removing punctuation in the word frequency counter.

Setting up the logic to seamlessly take comma-separated score inputs from the user in the terminal and convert them into a list.

Key Learning
Error handling (try-except) is essential for preventing application crashes.

Learned how to effectively use Python dictionaries and list methods for data aggregation.

Realized the benefits of keeping code clean, readable, and modular.

Author
Student Name: Muhammad Jahanzaib Azhar

Internship Program: AI Internship 

Date: 20 June 2026
